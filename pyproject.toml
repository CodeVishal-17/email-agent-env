[project]
name = "email-agent-env"
version = "0.1.0"
description = "AI Email RL Environment"
authors = [{ name = "Vishal" }]
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai",
    "openenv-core"
]

[project.scripts]
server = "server.app:main"